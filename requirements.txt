# TMT Translator — Python dependencies
# Install: pip install -r requirements.txt

# ── Web framework ─────────────────────
flask==2.3.3
gunicorn==21.2.0          # production WSGI server

# ── HTTP / API ────────────────────────
requests==2.31.0
python-dotenv==1.0.0

# ── PDF processing ────────────────────
pdfplumber==0.9.0
pymupdf==1.23.8
weasyprint==60.2          # Devanagari-aware PDF reconstruction

# ── DOCX processing ───────────────────
python-docx==0.8.11

# ── CSV / spreadsheet ─────────────────
openpyxl==3.1.2
